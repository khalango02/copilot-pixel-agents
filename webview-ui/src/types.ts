// Type-only import: one wire contract, no extension/VS Code code in the browser.
export type {
  ToolStatus, ToolPayload, TaskDetails, ToolHistoryEntry, AgentSnapshot,
  ServerMessage, ClientMessage,
} from '../../src/types.js';
